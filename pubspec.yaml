name: verb_app
description: "A local-first personal task manager for Android & Windows."
publish_to: "none"
version: 0.4.4+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.0
  rrule: ^0.2.18
  http: ^1.2.0
  uuid: ^4.5.0
  flutter_local_notifications: ^19.0.0
  timezone: ^0.10.0
  path_provider: ^2.1.0
  workmanager: ^0.7.0
  tray_manager: ^0.3.1
  window_manager: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true

















